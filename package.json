{
  "name": "youtube-downloader",
  "version": "1.0.0",
  "description": "YouTube Downloader with React and Node.js",
  "scripts": {
    "start": "cd api && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd api && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=14.x"
  }
}
